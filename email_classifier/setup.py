from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A simple ml model to classify email as spam or legitimate email',
    author='vivek',
    license='',
)
